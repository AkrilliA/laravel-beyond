<?php

namespace {{ namespace }};

use Closure;
use Illuminate\Http\Request;

final class {{ className }}
{
    public function handle(Request $request, Closure $next)
    {
        return $next($request);
    }
}
